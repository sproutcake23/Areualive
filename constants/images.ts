// Centralized image imports. Never import images directly inside screens
// or components — always go through this file. See AGENTS.md → Image Rules.
import icon from "@/assets/images/icon.png";

export const images = {
  icon,
};
