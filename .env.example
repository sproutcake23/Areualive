# Copy to .env and fill in real values. .env is gitignored; this file is committed.
# AWS API Gateway endpoint the sync service POSTs attendance records to.
# Read in constants/config.ts as config.SYNC_ENDPOINT.
EXPO_PUBLIC_SYNC_ENDPOINT=
